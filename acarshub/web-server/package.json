{
  "name": "web-server",
  "version": "4.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "start-dev": "npm run clean && nodemon ./src/index.ts",
    "start-prod": "npm-run-all clean build-prod && node dist/index.js",
    "build-dev": "npm run clean && tsc",
    "build-prod": "npm run clean && tsc"
  },
  "keywords": [],
  "author": "Fred Clausen",
  "license": "GPL-3.0-only",
  "devDependencies": {
    "@types/express": "^4.17.14",
    "@types/node": "^18.8.3",
    "express": "^4.18.1"
  }
}
